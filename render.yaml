services:
  - type: web
    name: vya-pyaar-backend
    env: node
    plan: free
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
